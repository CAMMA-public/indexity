import { SizeSensorDirective } from './size-sensor.directive';

describe('SizeSensorDirective', () => {
  it('should create an instance', () => {
    const directive = new SizeSensorDirective({
      nativeElement: undefined
    });
    expect(directive).toBeTruthy();
  });
});
